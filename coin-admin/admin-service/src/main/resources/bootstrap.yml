spring:
  application:  # 应用名称
    name: admin-service
  profiles:  #切换到开发环境上面
    active: dev
  cloud:
    nacos:  #配置Nacos的地址
      server-addr: 127.0.0.1:8848
      config:  # 配置文件的扩展名
        file-extension: yaml
